name: Build Marlin Firmwares

on:
  push:
    branches: ["main"]

jobs:
  build-firmwares:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - id: Generate-build-list
        run: for i in *; do test -d $i && echo $i; done
