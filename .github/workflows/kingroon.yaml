name: Build Kingroon Firmwares

on:
  pull_request:
    branches:
      - main
    paths:
      - builder.sh
      - configs/Kingroon/**

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      prefix: kingroon
      parent: Kingroon
