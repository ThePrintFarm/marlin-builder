name: Build Creality firmwares

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: ["main"]

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      prefix: creality
      parent: Creality
